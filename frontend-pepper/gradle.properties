org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true

# Backend base URL, read into BuildConfig.API_BASE_URL by app/build.gradle.
# Override here (or in a non-committed local override) to point at a different
# deployment, e.g. a laptop running the backend on the same WiFi as Pepper.
API_BASE_URL=https://otc-production-2802.up.railway.app
